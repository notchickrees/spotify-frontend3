{
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "morgan": "^1.10.0",
    "pg": "^8.8.0"
  },

  "scripts": {
    "start": "cd server && npm install && node app.js",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  }
}
